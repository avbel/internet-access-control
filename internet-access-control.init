#!/bin/sh /etc/rc.common

START=99
STOP=10

USE_PROCD=1

PROG=/usr/bin/internet-access-control
CONFIG_FILE=/etc/internet-access-control.yml

start_service() {
	procd_open_instance
	procd_set_param command "$PROG"
	procd_append_param command --config "$CONFIG_FILE"
	procd_append_param command --port 8080
	procd_append_param command --wan-interface wan
	procd_append_param command --bind 0.0.0.0

	procd_set_param respawn ${respawn_threshold:-3600} ${respawn_timeout:-5} ${respawn_retry:-5}
	procd_set_param stdout 1
	procd_set_param stderr 1

	procd_close_instance
}

stop_service() {
	service_stop "$PROG"
}

reload_service() {
	stop
	start
}
